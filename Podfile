# Uncomment the next line to define a global platform for your project
# platform :ios, '11.0'

target 'Flight Tracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Flight Tracker
pod 'MapboxSceneKit', :git => 'https://github.com/mapbox/mapbox-scenekit.git', :branch => 'queueAndFetchesCrashFixes'
pod 'MapboxMobileEvents'
pod 'Mapbox-iOS-SDK'

  target 'Flight TrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Flight TrackerUITests' do
    # Pods for testing
  end

end
